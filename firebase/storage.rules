rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Bootstrap posture: private by default.
    // Add company/project-scoped rules only with emulator tests.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
